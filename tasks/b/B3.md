# B3 - resize usage menu and keep website downloads current
[Overview](../BASED.md)

Fix retained menu height after provider changes and automate the website's latest download, then publish a patch release.

## Context

The window-style menu retains empty space after disabling providers in Settings. `MenuBarRootView` fixes width but accepts the host's proposed height. The website already links to `/releases/latest/download/AIUsageMonitor.dmg`, but releases publish only a versioned DMG, leaving that URL broken.

## Scope

- Content-driven menu sizing and regression coverage.
- A stable download asset in each release, retaining the existing versioned asset.
- Patch version, release publication, and live download verification.

## Non-goals

- Redesigning the menu, changing Settings presentation, or changing provider/auth behavior.
- Changing signing, supported platforms, or updater behavior.

## Acceptance Criteria

- [x] Menu content shrinks/grows with provider selection, including zero providers and named OpenAI accounts, despite a previous height proposal.
- [ ] The website's existing download URL serves the current release DMG and future releases automatically include that asset.
- [ ] Tests and release build pass; a new patch release is published.

## Plan

1. Reproduce retained height with a layout regression, then constrain the menu to its ideal vertical size.
2. Publish a stable-name alias of the versioned DMG and document the download contract.
3. Run tests/site build/packaging, bump to 0.1.22, push, and verify GitHub release and live website download.

## TODOS

- [x] Add and verify resizing regression tests and fix.
- [x] Automate stable download asset and document the contract.
- [ ] Verify, publish 0.1.22, and record release results.

## Verification

- `swift test`
  - Passed: 69 tests, including retained-height proposals, named-account/detail changes, and an AppKit-hosted window shrinking while visible and after reopening.
- Temporary isolated `MenuBarExtra` smoke probe compiled against production Sources (excluding the production app entry point), with empty provider clients and no persisted setting changes:
  - `swiftc -parse-as-library -o /tmp/MenuSizingProbe $(find Sources -name '*.swift' ! -name AIUsageMonitorApp.swift) /tmp/MenuSizingProbe.swift -lsqlite3 && /tmp/MenuSizingProbe`
  - Passed on macOS 26.2: actual menu window measured 340×281 with five pending providers, 340×241 after disabling one while closed and opening Settings, 340×129 with none, and 340×281 after re-enabling five. Output: `/tmp/menu-sizing-after.log`.
  - Limitation: the simplified baseline probe and new unit tests also passed before the modifier; the user's environment-specific retained-height failure was not reproduced locally. The fix explicitly enforces ideal vertical sizing rather than relying on host proposals.
- `bash Scripts/build_dmg.sh && cmp dist/AIUsageMonitor-0.1.22.dmg dist/AIUsageMonitor.dmg`
  - Passed: release build, ad-hoc signing, DMG packaging, identical download alias; bundle version 0.1.22/build 22.
- `npm --prefix web run build`
  - Passed: static website build.
- `python3 scripts/validate_based.py`, `bash -n Scripts/build_dmg.sh`, `git diff --check`
  - Passed.
- GitHub release and public download verification: pending push/publication.

## NOTES

- User explicitly authorized publication without further confirmation.
- Keep GitHub's latest-release redirect rather than embedding a version into the static website; this avoids deploy/release ordering races. See [ledger decision 0004](../../ledger/0004-publish-stable-latest-download-asset.md).
- The release workflow now runs tests before packaging, verifies the stable alias, and explicitly tags the triggering commit.

## LOGS

- 2026-09-18 — Confirmed v0.1.21 has only `AIUsageMonitor-0.1.21.dmg`; website requests a nonexistent stable asset. Began focused layout and release fixes.
- 2026-09-18 — Added content-driven vertical sizing, three layout/window tests, and automatic stable DMG publication. All 69 tests, website build, release packaging, and real-menu smoke checks passed; prepared v0.1.22.

---
